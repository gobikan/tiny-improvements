import React from "react";
import { Col, Container, DropdownButton, Row, Form, FormGroup, Input, Label, Button, Card, CardBody } from "reactstrap";

const KudosFilter = props => (
    <Container>
        <Card>
            <CardBody>
                {/* <DropdownButton
                    title={props.title}

                >
                    <MenuItem eventKey="1">Action</MenuItem>
                    <MenuItem eventKey="2">Another action</MenuItem>
                </DropdownButton> */}

            </CardBody>
        </Card>
    </Container>

)

export default KudosFilter;